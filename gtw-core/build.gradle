plugins {
    id 'java'
}

group = mod_group
version = mod_version

allprojects {
    dependencies {
        compileOnly project(':gtw-api')
    }
}

repositories {
    mavenCentral()
}

dependencies {
}
